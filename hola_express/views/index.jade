doctype html
html(lang='es')
  head
    title Mi primera pagina con express
  body

    h1!=titulo

    //comentario
    
    -var arreglo = [1,2,3,4,5];
    -for (var i=arreglo.length-1; i>=0; i--)
        p=arreglo[i]

    -var variable = "Una variable"
    p="<h1>"+variable+"</h1>"

    p.
        Haz click #[a(href="/mienlace") aqui] por favor

    each item in arreglo
        p=item

    each item in arreglo
        case item
            when 1
                p El item es 1
            when 2
                p El item es 2    
            default
                p El item no es 1 ni 2